---
defaults:
  machines:
    core: &machine_core
      providers:
        virtualbox:
        hyperv:
      provisioners:
        shell:
          inline: echo Hello World!

environment: shell.provisioning.vagrant

machines:
  linux:
    <<: *machine_core
    box: gusztavvargadr/u16s
  windows:
    <<: *machine_core
    box: gusztavvargadr/w16s
