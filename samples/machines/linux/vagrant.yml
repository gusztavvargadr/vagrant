---
defaults:
  machines:
    core: &machine_core
      providers:
        virtualbox:
        hyperv:

environment: linux.machines.vagrant

machines:
  u16s:
    <<: *machine_core
    box: gusztavvargadr/u16s
