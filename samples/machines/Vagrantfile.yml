component: machines

machines:
  # defaults:
  #   primary: false
  #   autostart: true
  #   count: 1
  windows:
    # primary: false
    # autostart: true
    # count: 1
  linux:
    # primary: false
    # autostart: true
    # count: 1
