pr: none

stages:
  - template: ../../../build/azp/azure-pipelines.stages.yml
    parameters:
      configurations:
        - name: [ 'samples', 'machines', 'single' ]
          machines: [ 'default' ]
          providers: [ 'virtualbox', 'hyperv' ]
