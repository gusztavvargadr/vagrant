trigger:
  branches:
    include:
      - '*'
  tags:
    include:
      - '*'
  paths:
    include:
      - samples/machines/multiple/desktop
pr: none

stages:
  - template: ../../../../build/azp/azure-pipelines.stages.yml
    parameters:
      configurations:
        - name: [ 'samples', 'machines', 'multiple', 'desktop' ]
          machines: [ 'windows', 'linux' ]
          providers: [ 'virtualbox', 'hyperv' ]
