vms:
  default:
    box: ubuntu/trusty64
    provisioners:
      00-file:
        type: file
        source: ~/.gitconfig
        destination: /home/vagrant/.gitconfig
      10-chef-solo:
        type: chef-solo
        recipes:
          - hello_world::default
        attributes:
      20-reload:
        type: reload
