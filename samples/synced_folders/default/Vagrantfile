directory = File.dirname(__FILE__)

require "#{directory}/../vagrant"

VagrantMachine.defaults_include(
  'synced_folders' => {
    '/vagrant-default' => {
      'source' => '.',
    },
  }
)

VagrantDeployment.configure(directory, 'component' => 'default', 'stack' => 'synced_folders')
