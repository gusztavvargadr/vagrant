environment: linux.synced_folders.vagrant

machines:
  default:
    synced_folders:
      /vagrant-local:
  smb:
    synced_folders:
      /vagrant-local:
        type: 'smb'
  rsync:
    synced_folders:
      /vagrant-local:
        type: 'rsync'
