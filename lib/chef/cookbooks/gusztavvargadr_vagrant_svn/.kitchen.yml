---
driver:
  name: vagrant
  customize:
    memory: 4096
    cpus: 4

provisioner:
  name: chef_solo

platforms:
  - name: win10
    driver:
      box: gusztavvargadr/windows10ee

suites:
  - name: profiles
    run_list:
      - recipe[gusztavvargadr_vagrant_svn::profiles]
    attributes:
      gusztavvargadr_vagrant_svn:
        profiles:
          http://svn.apache.org/repos:
            checkout_directory_path: /Users/vagrant/Repos
            repositories:
              asf/logging/log4net/trunk:
