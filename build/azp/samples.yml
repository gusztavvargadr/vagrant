trigger: none
pr: none

stages:
- stage: build
  displayName: Build

  jobs:
  - template: ./samples.jobs.build.yml
    parameters:
      samples:
      - name: machines
      - name: provisioners
      machines:
      - windows
      - linux
      providers:
      - virtualbox
      - hyperv
