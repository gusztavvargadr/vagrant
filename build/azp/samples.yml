trigger: none
pr: none

stages:
- stage: build
  displayName: Build

  jobs:
  - template: ./jobs.build.yml
    parameters:
      samples:
      - name:
        - machines
        - server
      - name:
        - provisioners
        - shell
      machines:
      - linux
      providers:
      - virtualbox
